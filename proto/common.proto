syntax = "proto3";

package proto;

option go_package = "proto/commonpb";

message Error {
    int32 code = 1;
    string msg = 2;
}

message ValueMeta {
    bool write_intent = 1;
    uint64 version = 2;
}

message Value {
    ValueMeta Meta = 1;
    bytes val = 2;
}

message ReadOption {
    uint64 version = 1;
    bool exact_read_version = 2;
}

message WriteOption {
    bool clear_write_intent = 1;
    bool remove_version = 2;
}
